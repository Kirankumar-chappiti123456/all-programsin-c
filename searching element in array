#include<stdio.h>
int main()
{
    int a[]={1,3,4,5,6,7,8,9},ch,f=0;
    printf("enter the number to search:");
    scanf("%d",&ch);
    int l=sizeof(a)/sizeof(a[0]);
    for(int i=0;i<l;i++)
    {
        if(a[i]==ch)
        f++;
    }
    (f==1)?printf("element found"):printf("not element");
}
output:
enter the number to search:1222
not element
output:
enter the number to search:9
element found
